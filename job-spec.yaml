workerPoolSpecs:
  machineSpec:
    machineType: n1-highcpu-16
  replicaCount: 1
  containerSpec:
    imageUri: gcr.io/warm-aegis-336012/mrnet-container
    args:
      - MRNet-v1.0
      - axial
      - --epochs=8
      - --backbone=resnet
      - --no-load-dataset
      - --device=cpu
